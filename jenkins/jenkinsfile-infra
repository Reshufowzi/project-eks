pipeline {
  agent any

  stages {

    stage('Terraform Init') {
      steps {
        dir('terraform_2') {
          sh 'terraform init'
        }
      }
    }

    stage('Terraform Apply') {
      steps {
        dir('terraform_2') {
          sh 'terraform apply -auto-approve'
        }
      }
    }
  }
}
